{
  "rules": {
    "usuarios": {
      "$uid": {
        "favoritos": {
          ".indexOn": ["id"]
        },
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}